# Public base URL for the backend API (FastAPI/CLI)
# Public base URL if calling backend directly from the browser (optional)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Server-side proxy target for Next.js rewrites (preferred)
API_BASE_URL=http://localhost:8000
